// TYPE conversion:
const inputYear = '1991';

console.log(Number(inputYear) + 18);
console.log(typeof String(Number(inputYear) + 18));

// TYPE coercion:
//      - javascript behind the scenes convert one of the other values 
//      to the same datatype so that calculation is possible

console.log('I am ' + 31 + ' years old');

